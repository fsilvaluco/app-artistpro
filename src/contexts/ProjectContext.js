// Contexto para el proyecto/artista seleccionado
import { createContext } from "react";

export const ProjectContext = createContext(null);

// Más adelante se agregará el provider y lógica de selección de proyecto.
